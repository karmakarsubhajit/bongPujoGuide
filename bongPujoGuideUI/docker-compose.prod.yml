services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
    container_name: bongpujoguide-frontend
    restart: unless-stopped
    networks:
      - web

networks:
  web:
